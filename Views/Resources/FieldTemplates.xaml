<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:vm="clr-namespace:PautaDinamicaApp.ViewModels"
                    xmlns:models="clr-namespace:PautaDinamicaApp.Models"
                    xmlns:conv="clr-namespace:PautaDinamicaApp.Views.Converters">

    <conv:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

    <!-- Paleta de Colores Centralizada -->
    <SolidColorBrush x:Key="InputBackground" Color="#2d3446"/>
    <SolidColorBrush x:Key="InputForeground" Color="#e1e1e1"/>
    <SolidColorBrush x:Key="InputBorder" Color="#3d4456"/>
    <SolidColorBrush x:Key="ErrorBrush" Color="#ff4d4d"/>
    <SolidColorBrush x:Key="AccentBrush" Color="#007bff"/>
    <SolidColorBrush x:Key="SecondaryTextBrush" Color="#94a3b8"/>

    <!-- ESTILOS DE BOTONES MODERNOS -->
    <Style x:Key="ModernButtonBase" TargetType="Button">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Padding" Value="15,8"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.4"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ModernButtonBase}">
        <Setter Property="Background" Value="#007bff"/>
    </Style>
    <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource ModernButtonBase}">
        <Setter Property="Background" Value="#28a745"/>
    </Style>
    <Style x:Key="ExcelButton" TargetType="Button" BasedOn="{StaticResource ModernButtonBase}">
        <Setter Property="Background" Value="#217346"/>
    </Style>
    <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ModernButtonBase}">
        <Setter Property="Background" Value="#dc3545"/>
    </Style>
    <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButtonBase}">
        <Setter Property="Background" Value="#6f42c1"/>
    </Style>
    <Style x:Key="GhostButton" TargetType="Button" BasedOn="{StaticResource ModernButtonBase}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="#e1e1e1"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#4a5568"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Label Template with Required Asterisk -->
    <DataTemplate x:Key="FieldLabelTemplate">
        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
            <TextBlock Text="{Binding Label}" Foreground="#94a3b8" FontSize="12"/>
            <TextBlock Text=" *" Foreground="{StaticResource ErrorBrush}" FontSize="12" 
                       Visibility="{Binding IsRequired, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </StackPanel>
    </DataTemplate>

    <!-- Error Message Template -->
    <DataTemplate x:Key="FieldErrorTemplate">
        <TextBlock Text="{Binding ValidationError}" Foreground="{StaticResource ErrorBrush}" 
                   FontSize="10" Margin="0,2,0,0"
                   Visibility="{Binding IsValid, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}"/>
    </DataTemplate>

    <!-- Estilo para TextBox -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource InputBackground}"/>
        <Setter Property="Foreground" Value="{StaticResource InputForeground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource InputBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,5"/>
        <Setter Property="Height" Value="35"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="CaretBrush" Value="White"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="border" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4">
                        <ScrollViewer x:Name="PART_ContentHost"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding IsValid, FallbackValue=True}" Value="False">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource ErrorBrush}"/>
                            <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Estilo para ComboBox -->
    <Style TargetType="ComboBox">
        <Setter Property="Height" Value="35"/>
        <Setter Property="Padding" Value="8,5"/>
        <Setter Property="Foreground" Value="{StaticResource InputForeground}"/>
        <Setter Property="Background" Value="{StaticResource InputBackground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource InputBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton Name="ToggleButton"
                                      BorderBrush="{TemplateBinding BorderBrush}"
                                      Background="{TemplateBinding Background}"
                                      Foreground="{TemplateBinding Foreground}"
                                      Grid.Column="2" 
                                      Focusable="false"
                                      IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Name="border" Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="4">
                                        <Grid HorizontalAlignment="Right">
                                            <Path Name="Arrow" 
                                                  Fill="{TemplateBinding Foreground}"
                                                  Data="M 0 0 L 4 4 L 8 0 Z"
                                                  Margin="0,0,10,0"
                                                  VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <DataTrigger Binding="{Binding IsValid, FallbackValue=True}" Value="False">
                                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource ErrorBrush}"/>
                                            <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                                        </DataTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <ContentPresenter Name="ContentSite"
                                          IsHitTestVisible="False" 
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          Margin="10,3,30,3"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Left" />
                        <Popup Name="Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True" 
                               Focusable="False"
                               PopupAnimation="Slide">
                            <Grid Name="DropDown"
                                  SnapsToDevicePixels="True"                
                                  MinWidth="{TemplateBinding ActualWidth}"
                                  MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <Border Name="DropDownBorder"
                                        Background="{StaticResource InputBackground}"
                                        BorderThickness="1"
                                        BorderBrush="{StaticResource InputBorder}"
                                        CornerRadius="0,0,4,4"/>
                                <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                </ScrollViewer>
                            </Grid>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="DatePicker">
        <Setter Property="Height" Value="35"/>
        <Setter Property="Background" Value="{StaticResource InputBackground}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DatePicker">
                    <Border Name="border" Background="{TemplateBinding Background}" 
                            BorderBrush="{StaticResource InputBorder}" 
                            BorderThickness="1" CornerRadius="4">
                        <Grid x:Name="PART_Root">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <DatePickerTextBox x:Name="PART_TextBox" Grid.Column="0" 
                                               Background="Transparent" Foreground="{StaticResource InputForeground}"
                                               BorderThickness="0" VerticalContentAlignment="Center" Padding="5,0"/>
                            <Button x:Name="PART_Button" Grid.Column="1" HorizontalAlignment="Right"
                                    VerticalAlignment="Center" Width="20" Margin="0,0,5,0"
                                    Content="📅" Background="Transparent" BorderThickness="0" Foreground="{StaticResource InputForeground}"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding IsValid}" Value="False">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource ErrorBrush}"/>
                            <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Template para Texto -->
    <DataTemplate x:Key="TextTemplate" DataType="{x:Type vm:DynamicFieldVM}">
        <StackPanel Margin="0,5">
            <ContentControl Content="{Binding}" ContentTemplate="{StaticResource FieldLabelTemplate}"/>
            <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"/>
            <ContentControl Content="{Binding}" ContentTemplate="{StaticResource FieldErrorTemplate}"/>
        </StackPanel>
    </DataTemplate>

    <!-- Template para Numérico -->
    <DataTemplate x:Key="NumericTemplate" DataType="{x:Type vm:DynamicFieldVM}">
        <StackPanel Margin="0,5">
            <ContentControl Content="{Binding}" ContentTemplate="{StaticResource FieldLabelTemplate}"/>
            <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"/>
            <ContentControl Content="{Binding}" ContentTemplate="{StaticResource FieldErrorTemplate}"/>
        </StackPanel>
    </DataTemplate>

    <!-- Template para Booleano -->
    <DataTemplate x:Key="BooleanTemplate" DataType="{x:Type vm:DynamicFieldVM}">
        <StackPanel Margin="0,5">
            <CheckBox Content="{Binding Label}" 
                      IsChecked="{Binding Value}" 
                      Foreground="{StaticResource InputForeground}"
                      Margin="0,10"
                      VerticalContentAlignment="Center"/>
            <ContentControl Content="{Binding}" ContentTemplate="{StaticResource FieldErrorTemplate}"/>
        </StackPanel>
    </DataTemplate>

    <!-- Template para Dropdown -->
    <DataTemplate x:Key="DropdownTemplate" DataType="{x:Type vm:DynamicFieldVM}">
        <StackPanel Margin="0,5">
            <ContentControl Content="{Binding}" ContentTemplate="{StaticResource FieldLabelTemplate}"/>
            <ComboBox ItemsSource="{Binding Options}" 
                      SelectedItem="{Binding Value}"/>
            <ContentControl Content="{Binding}" ContentTemplate="{StaticResource FieldErrorTemplate}"/>
        </StackPanel>
    </DataTemplate>

    <!-- Template para Fecha -->
    <DataTemplate x:Key="DateTemplate" DataType="{x:Type vm:DynamicFieldVM}">
        <StackPanel Margin="0,5">
            <ContentControl Content="{Binding}" ContentTemplate="{StaticResource FieldLabelTemplate}"/>
            <DatePicker SelectedDate="{Binding Value}"/>
            <ContentControl Content="{Binding}" ContentTemplate="{StaticResource FieldErrorTemplate}"/>
        </StackPanel>
    </DataTemplate>

    <!-- Template para Separador (Subcabecera dentro de un cuadro) -->
    <DataTemplate x:Key="SeparatorTemplate" DataType="{x:Type vm:DynamicFieldVM}">
        <StackPanel Margin="0,15,0,5">
            <TextBlock Text="{Binding Label}" Foreground="{StaticResource AccentBrush}" FontWeight="Bold" FontSize="14"/>
            <Rectangle Height="1" Fill="#3d4456" Margin="0,5,0,0"/>
        </StackPanel>
    </DataTemplate>

    <!-- Template para Cálculo / Porcentaje (Solo Lectura) -->
    <DataTemplate x:Key="CalculationTemplate" DataType="{x:Type vm:DynamicFieldVM}">
        <StackPanel Margin="0,5">
            <ContentControl Content="{Binding}" ContentTemplate="{StaticResource FieldLabelTemplate}"/>
            <Border Background="#1a1f2b" CornerRadius="4" Padding="10,8" BorderBrush="#007bff" BorderThickness="1,1,1,2">
                <TextBlock Text="{Binding Value, TargetNullValue='0.00%'}" Foreground="#007bff" FontWeight="Bold" FontSize="14" HorizontalAlignment="Right"/>
            </Border>
        </StackPanel>
    </DataTemplate>

    <!-- Template para Promedio (Solo Lectura) -->
    <DataTemplate x:Key="AverageTemplate" DataType="{x:Type vm:DynamicFieldVM}">
        <StackPanel Margin="0,5">
            <ContentControl Content="{Binding}" ContentTemplate="{StaticResource FieldLabelTemplate}"/>
            <Border Background="#1a1f2b" CornerRadius="4" Padding="10,8" BorderBrush="#6f42c1" BorderThickness="1,1,1,2">
                <TextBlock Text="{Binding Value, TargetNullValue='0.00%'}" Foreground="#6f42c1" FontWeight="Bold" FontSize="14" HorizontalAlignment="Right"/>
            </Border>
        </StackPanel>
    </DataTemplate>

</ResourceDictionary>

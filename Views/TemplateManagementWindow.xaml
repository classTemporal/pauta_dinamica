<Window x:Class="PautaDinamicaApp.Views.TemplateManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:PautaDinamicaApp.Views.Converters"
        Title="Gestión de Plantillas" Height="650" Width="800"
        WindowStartupLocation="CenterOwner" Background="{DynamicResource BackgroundBrush}" Foreground="{DynamicResource TextBrush}">
    
    <Window.Resources>
        <conv:BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <conv:BindingProxy x:Key="Proxy" Data="{Binding}"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Cabecera -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Gestión de Plantillas de Texto" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}"/>
            <TextBlock Text="Agrega textos predefinidos para insertar rápidamente en campos cortos o largos." FontSize="13" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Agregar Nueva Plantilla -->
        <Border Grid.Row="1" Background="{DynamicResource CardBackgroundBrush}" Padding="15" CornerRadius="8" Margin="0,0,0,15" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Text="Nueva Plantilla (Contenido):" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,8"/>
                
                <TextBox Grid.Row="1" Grid.Column="0" Text="{Binding NewTemplateContent, UpdateSourceTrigger=PropertyChanged}" 
                         Foreground="{DynamicResource TextBrush}" Background="{DynamicResource InputBackground}"
                         AcceptsReturn="True" Height="100" VerticalScrollBarVisibility="Auto" 
                         TextWrapping="Wrap" Margin="0,0,15,0" Padding="10"/>
                
                <Button Grid.Row="1" Grid.Column="1" Content="➕ AGREGAR" Command="{Binding AddTemplateCommand}" 
                        Style="{StaticResource SuccessButton}" Width="120" Height="100" VerticalAlignment="Bottom"/>
            </Grid>
        </Border>

        <!-- Lista de Plantillas -->
        <DockPanel Grid.Row="2">
            <Border DockPanel.Dock="Top" Background="{DynamicResource CardBackgroundBrush}" Padding="10" CornerRadius="8,8,0,0" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1,1,1,0">
                <Grid>
                    <!-- Vista Normal -->
                    <StackPanel Orientation="Horizontal" Visibility="{Binding IsMultiSelectMode, Converter={StaticResource BoolToVis}, ConverterParameter=Inverse}">
                        <Button Content="☑️ Selección Múltiple" Command="{Binding ToggleMultiSelectCommand}" 
                                Style="{StaticResource GhostButton}" Padding="15,5" Height="34" Margin="0,0,10,0"/>
                        <Button Content="📥 Importar Excel" Command="{Binding ImportExcelCommand}" 
                                Style="{StaticResource GhostButton}" Padding="15,5" Height="34" Margin="0,0,10,0"/>
                        <Button Content="📤 Exportar Todo" Command="{Binding ExportExcelCommand}" 
                                Style="{StaticResource SecondaryButton}" Padding="15,5" Height="34"/>
                    </StackPanel>

                    <!-- Vista Selección Múltiple -->
                    <StackPanel Orientation="Horizontal" Visibility="{Binding IsMultiSelectMode, Converter={StaticResource BoolToVis}}">
                        <Button Content="✔️ Seleccionar Todo" Command="{Binding SelectAllCommand}" 
                                Style="{StaticResource GhostButton}" Padding="15,5" Height="34" Margin="0,0,10,0"/>
                        <Button Content="📤 Exportar Seleccionados" Command="{Binding ExportExcelCommand}" 
                                Style="{StaticResource SecondaryButton}" Padding="15,5" Height="34" Margin="0,0,10,0"/>
                        <Button Content="🗑️ Eliminar Seleccionados" Command="{Binding DeleteSelectedCommand}" 
                                Style="{StaticResource DangerButton}" Padding="15,5" Height="34" Margin="0,0,10,0"/>
                        <Button Content="❌ Salir Selección" Command="{Binding ToggleMultiSelectCommand}" 
                                Style="{StaticResource GhostButton}" Padding="15,5" Height="34"/>
                    </StackPanel>
                </Grid>
            </Border>

            <DataGrid ItemsSource="{Binding Templates}" AutoGenerateColumns="False" CanUserAddRows="False"
                      Background="Transparent" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1,0,1,1"
                      GridLinesVisibility="Horizontal">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="MinHeight" Value="50"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource BorderBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTemplateColumn Width="45" Visibility="{Binding Data.IsMultiSelectMode, Source={StaticResource Proxy}, Converter={StaticResource BoolToVis}}">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Contenido de la Plantilla" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding Model.Content, UpdateSourceTrigger=PropertyChanged}" 
                                         AcceptsReturn="True" TextWrapping="Wrap" BorderThickness="0" Background="Transparent" Padding="10" 
                                         Foreground="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=Foreground}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Width="50" Visibility="{Binding Data.IsMultiSelectMode, Source={StaticResource Proxy}, Converter={StaticResource BoolToVis}, ConverterParameter=Inverse}">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="🗑️" Command="{Binding DataContext.DeleteTemplateCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                        CommandParameter="{Binding}" Foreground="{DynamicResource DangerBrush}" Background="Transparent" BorderThickness="0" FontSize="18" Padding="10"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>

        <!-- Pie -->
        <Border Grid.Row="3" Margin="0,20,0,0" Padding="0,15,0,0" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="CANCELAR" Command="{Binding CancelCommand}" 
                        Style="{StaticResource GhostButton}" Width="150" Height="45" Margin="0,0,15,0"/>
                <Button Content="💾 GUARDAR CAMBIOS" Command="{Binding SaveChangesCommand}" 
                        Style="{StaticResource SuccessButton}" Width="220" Height="45"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>

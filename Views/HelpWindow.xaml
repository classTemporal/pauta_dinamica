<Window x:Class="PautaDinamicaApp.Views.HelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" Height="600" Width="800"
        WindowStartupLocation="CenterOwner" 
        Background="{DynamicResource BackgroundBrush}" 
        Foreground="{DynamicResource TextBrush}">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="0,0,0,10" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Margin="0,0,0,10">
            <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}"/>
        </Border>

        <WebBrowser Grid.Row="1" x:Name="HelpBrowser" Navigating="WebBrowser_Navigating"/>

        <Button Grid.Row="2" Content="Cerrar" Click="Close_Click" 
                HorizontalAlignment="Right" Margin="0,15,0,0"
                Padding="25,8" Style="{StaticResource PrimaryButton}"/>
    </Grid>
</Window>
